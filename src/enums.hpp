#pragma once

// Enum of possible scenes in game
enum class eSCENE {
	MAIN_MENU,
	SETTINGS,
	BATTLE,
	EDIT
};

// Enum of possible button presses
enum class eBUTTON {
	SELECT,
	CANCEL,
	ESCAPE,
	UP,
	DOWN,
	RIGHT,
	LEFT,
	SPECIAL1,
	SPECIAL2,
	NONE
};
